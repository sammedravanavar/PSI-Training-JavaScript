'use strict';
var initValue;
initValue = 5;

function counter(param1) {
    // if (initValue > 0) {
    //     console.log(initValue);
    //     initValue--;
    //     callback(callback);
    // }
}

function init(cb){
    
   cb(10);
}
init(counter);